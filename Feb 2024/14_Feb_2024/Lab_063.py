# Reverse Indexation: Suppose we have original string = "Vandana" and we can reverse the string by reverse indexing
# which means original square brackets zero. In this case, "p" will be printed. we can give -1 which will do a
# reverse of the string.
original_string = input("Enter string\n")
rev_str = original_string[::-1]
print(rev_str)
