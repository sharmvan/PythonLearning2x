# functions
# 1. builtin functions like: print(), sum(),max(), input() etc
result = max(2, 8)  # For built-in functions, we don't need to define them. We just call them.
print(result)
num = int(input("Enter any number: \n"))  # For built-in functions, we don't need to define them. We just call them.
import math

print(f"The factorial of {num} is:", math.factorial(num))  # For built-in functions, we don't need to define them.
# We just call them.